<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% if title %}{{ title }} | {% endif %}Nelleke Strik - Counselling & Psychotherapy</title>
    <meta name="description" content="{% if description %}{{ description }}{% else %}Nelleke Strik offers professional counselling and psychotherapy in a safe, supportive, and non-judgmental space.{% endif %}">
    <link rel="stylesheet" href="{{ '/css/main.css' | url }}">
    {% if pageStyles %}<link rel="stylesheet" href="{{ '/css/' + pageStyles | url }}">{% endif %}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@300;400;600&display=swap" rel="stylesheet">
</head>
<body>
    {% include "partials/header.njk" %}

    <main>
        {{ content | safe }}
    </main>

    {% include "partials/footer.njk" %}

    <!-- Notice the use of the URL filter to handle the path prefix -->
    <script src="{{ '/js/main.js' | url }}"></script>
    {% if pageScripts %}<script src="{{ '/js/' + pageScripts | url }}"></script>{% endif %}
</body>
</html>
